-- Relax setuptools requirement for older setuptools compatibility.

--- pyproject.toml.orig	2026-07-20 18:32:23 UTC
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools>=64", "setuptools-scm>=8.0"]
+requires = ["setuptools>=61", "setuptools-scm>=8.0"]
 build-backend = "setuptools.build_meta"
 
 [project]
@@ -11,7 +11,7 @@ readme = "README.rst"
 requires-python = ">=3.9"
 description = "Super-fast and clean conversions to numbers."
 readme = "README.rst"
-license = "MIT"
+license = {text = "MIT"}
 keywords = ["conversion", "numeric", "performance"]
 classifiers = [
 	"Development Status :: 5 - Production/Stable",
